POSTGRES_DB=volatile_signals_26
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_HOST=db
POSTGRES_PORT=5432
SECRET_KEY=change-me
DEBUG=True
# Comma-separated hostnames/IPs allowed in the Host header, e.g.
# 192.168.1.42,example.com. Django only auto-allows localhost/127.0.0.1/[::1]
# when this is left empty -- once you set it, list those too if you still
# need them.
ALLOWED_HOSTS=
MQTT_HOST=broker
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_TLS=False
# Address devices use to reach this server for OTA downloads, e.g.
# http://192.168.1.42:8000 -- must be reachable from the device's WiFi.
OTA_BASE_URL=